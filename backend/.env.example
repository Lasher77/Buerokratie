# Backend-Umgebungsvariablen
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an

# Server-Konfiguration
PORT=5000
# Wenn Frontend & API vom gleichen Host bedient werden, ist CORS nicht nötig.
# Für lokale Entwicklung kann http://localhost:3000 belassen werden.
ALLOWED_ORIGINS=http://localhost:3000

# HTTPS-Erzwingung (hinter Cloudflare empfohlen)
# true/1/yes aktiviert Redirect auf HTTPS, sobald x-forwarded-proto != https ist.
ENFORCE_HTTPS=true
NODE_ENV=production

# Datenbank-Konfiguration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=bvmw_user
DB_PASSWORD=changeme
DB_NAME=buerokratieabbau

# JWT-Konfiguration
JWT_SECRET=changeme
