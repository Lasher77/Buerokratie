# =============================================================================
# BVMW Bürokratieabbau-Plattform - Backend-Konfiguration
# =============================================================================
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an
#
# WICHTIG: Niemals .env in Git einchecken!
# =============================================================================

# -----------------------------------------------------------------------------
# Server-Konfiguration
# -----------------------------------------------------------------------------
PORT=5000
NODE_ENV=development

# -----------------------------------------------------------------------------
# CORS-Konfiguration
# -----------------------------------------------------------------------------
# Erlaubte Origins für API-Zugriffe (komma-getrennt)
#
# Entwicklung (Frontend auf Port 3000):
#   ALLOWED_ORIGINS=http://localhost:3000
#
# Produktion (Same-Origin - Frontend wird vom Backend ausgeliefert):
#   ALLOWED_ORIGINS=https://ihre-domain.de
#
# Produktion (Cross-Origin - Frontend auf anderer Domain):
#   ALLOWED_ORIGINS=https://frontend.ihre-domain.de,https://ihre-domain.de
#
ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# HTTPS-Konfiguration
# -----------------------------------------------------------------------------
# true/1/yes aktiviert Redirect auf HTTPS (empfohlen hinter Cloudflare/Nginx)
# In Entwicklung auf false setzen
ENFORCE_HTTPS=false

# -----------------------------------------------------------------------------
# Datenbank-Konfiguration
# -----------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=bvmw_user
DB_PASSWORD=changeme
DB_NAME=buerokratieabbau

# -----------------------------------------------------------------------------
# JWT-Konfiguration
# -----------------------------------------------------------------------------
# Geheimer Schlüssel für Token-Signierung
# WICHTIG: In Produktion einen langen, zufälligen String verwenden!
# Beispiel generieren: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=changeme

# Token-Gültigkeit (Standard: 24 Stunden)
JWT_EXPIRES_IN=24h
